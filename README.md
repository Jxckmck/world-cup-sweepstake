# World Cup Sweepstake 2026

Free live leaderboard website using GitHub Pages + GitHub Actions + football-data.org.

Upload all files, add the `FOOTBALL_DATA_TOKEN` GitHub secret, enable GitHub Pages, then run the updater from the Actions tab.
